//
//  Test.cpp
//  
//
//  Created by Steven Tang on 6/7/15.
//
//
/*
#include <iostream>
#include <string>
#include <vector> 
#include "Test.h"

using namespace std;

class Train
{
    vector<Car> cars;
public:
    void add_car(car c){ cars.push_back(c) }
    void remove_car(int n){
        for(int i = 0; i < cars.size(); i++){
            if(cars[i].get_number () == n){
                cars[i] == cars.back();
                cars.pop_back();
                return
            }
        }
    int passengers(){
        int total = 0;
        for ( int i = 0 ; i <cars.size(); i++){
            if(cars[i].get_type() == "passenger")
                total t = cars[i].get_capacity();
            return total
        }
            }
        }
    void print()
    { for(int i=0; i<cars.size(); i++)
        cars[i].print();
    }
};*/